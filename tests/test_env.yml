name: eomaps
channels:
  - conda-forge

dependencies:
  - numpy
  - scipy
  - pandas
  - geopandas
  - matplotlib >=3.4
  - descartes
  - mapclassify
  - pyproj
  - cartopy
  # --------------for data-shading
  - datashader
  # --------------for GeoTIFF and NetCDF files
  - netcdf4
  - xarray
  - rioxarray
  # --------------for WebMaps
  - owslib
  - requests
  - xmltodict
  - cairosvg
  # --------------for testing
  - nbformat     # to parse Jupyter Notebooks
  - coveralls
  - pytest
  - pytest-cov
  # --------------for testing the docs
  - docutils
  - jupyter
  - nbformat
